@using X.PagedList.Mvc.Core;
@using X.PagedList;
@model IEnumerable<FormuleCirkelEntity.Models.Engine>

@{
    ViewData["Title"] = "Engines";
}

<section>
    <h1>Engines</h1>
    <section>
        <div>
            <a asp-action="Create">
                <i class="fas fa-plus-square fa-2x"></i>
            </a>
            <a class="btn btn-dark float-right" asp-action="ArchivedEngines">Archive</a>
        </div>
    </section>
    <section>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Power</th>
                    <th>Available</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewBag.OnePage)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Power</td>
                        <td>@item.Available</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.EngineId">
                                <i class="fas fa-edit fa-2x"></i>
                            </a>
                        </td>
                        <td>
                            <a asp-action="Delete" asp-route-id="@item.EngineId">
                                <i class="fas fa-trash-alt fa-2x"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </section>
</section>

@Html.PagedListPager((IPagedList)ViewBag.OnePage, page => Url.Action("Index", new { page }),
    new X.PagedList.Mvc.Common.PagedListRenderOptionsBase {
        LiElementClasses = new string[] { "page-item" },
        PageClasses = new string[] { "page-link" }
    })