@model FormuleCirkelEntity.Models.Team

@{
    ViewData["Title"] = "Archive";
    string archive = "Archive";
    if (Model.Archived) { archive = "Unarchive"; }
}

<div class="d-inline-block">
    <h1>@archive</h1>
    <div>
        <h3>Are you sure you want to move @Model.Abbreviation?</h3>
        <dl class="row mt-3 text-left">
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Abbreviation)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.Abbreviation)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="@archive" class="btn fullwidth" />
        </form>
    </div>

    <div class="float-left mt-2">
        <a class="btn" asp-action="Index">
            <i class="fas fa-arrow-alt-circle-left fa-2x"></i> Back
        </a>
    </div>
</div>
