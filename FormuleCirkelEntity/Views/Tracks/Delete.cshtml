@model FormuleCirkelEntity.Models.Track

@{
    ViewData["Title"] = "Archive";
    string archive = "Archive";
    if (Model.Archived) { archive = "Unarchive"; }
}

<div class="d-inline-block">
    <h1>@archive</h1>
    <div>
        <h3>Are you sure you want to move @Model.Name?</h3>
        <dl class="row mt-3 text-left">
            <dt class="col-md-4">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-md-8">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class="col-md-4">
                @Html.DisplayNameFor(model => model.Location)
            </dt>
            <dd class="col-md-8">
                @Html.DisplayFor(model => model.Location)
            </dd>
            <dt class="col-md-4">
                @Html.DisplayNameFor(model => model.Specification)
            </dt>
            <dd class="col-md-8">
                @Html.DisplayFor(model => model.Specification)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="@archive" class="btn fullborder fullwidth" />
        </form>
    </div>

    <div class="float-left mt-2">
        <a class="btn" asp-action="Index">
            <i class="fas fa-arrow-alt-circle-left"></i> Back
        </a>
    </div>
</div>
