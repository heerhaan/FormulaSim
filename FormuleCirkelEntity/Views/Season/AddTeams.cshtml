@model IEnumerable<FormuleCirkelEntity.Models.Team>

@{
    ViewData["Title"] = "Voeg teams toe";
    int aantal = ViewBag.season;
}

<h1>Teams aan seizoen toevoegen</h1>
<h4>Huidige aantal deelnemende teams: @aantal</h4>

<table class="table">
    <thead>
        <tr>
            <th>
                Teamnaam
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="TeamToSeason" asp-route-id="@item.TeamId">Voeg aan kampioenschap toe.</a>
            </td>
        </tr>
}
    </tbody>
</table>
